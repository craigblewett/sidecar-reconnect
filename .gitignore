.DS_Store
build/
*.dSYM/

# Build output. CI builds and publishes it; committing it meant every merge
# conflicted on a binary that could only be resolved by rebuilding anyway.
/SidecarReconnect.app/
dist/
